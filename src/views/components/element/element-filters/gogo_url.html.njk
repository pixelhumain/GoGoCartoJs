{% if value %}
<div class="field field-url">
  {% set pending = value.indexOf('span class') > 0 %}
  {% if pending %}
    {{ value|safe }}
  {% else %}
   <a href="{{ value|add_protocol_if_need }}" target="_blank">{{ value|safe }}</a>
  {% endif %}
</div>
{% endif %}