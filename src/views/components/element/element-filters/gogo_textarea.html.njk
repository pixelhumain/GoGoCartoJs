{% if value %}
  {% set longText = splitLongText(value, truncate|default(1000), tolerance|default(50)) %}
  <div class="field field-textarea">
    <span>{{ longText.first|safe }}</span>
    {% if longText.second %}
      <span style="display:none" class="text-more">{{ longText.second|safe }}</span>  
      <a class="show-more">Afficher plus</a>
    {% endif %}
  </div>     
{% endif %}